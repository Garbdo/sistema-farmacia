import {Component,inject} from '@angular/core'; import {CommonModule} from '@angular/common'; import {FormsModule} from '@angular/forms'; import {ApiService} from './api.service';
@Component({standalone:true,imports:[CommonModule,FormsModule],template:`<div class="page-head"><div><div class="eyebrow">CATÁLOGO</div><h1>Categorías</h1><p>Clasificación del catálogo administrada desde la API.</p></div></div><section class="panel compact"><form class="inline-form" (ngSubmit)="create()"><input [(ngModel)]="name" name="name" placeholder="Nombre de nueva categoría" required><button class="btn primary">+ Crear</button></form><div class="tag-grid"><div class="category-card" *ngFor="let c of cats"><span>◈</span><div><b>{{c.nombre}}</b><small>{{c.activo?'Activa':'Inactiva'}}</small></div><button class="danger-link" (click)="remove(c.id)">Eliminar</button></div></div></section>`})
export class CategoriesComponent {api=inject(ApiService);cats:any[]=[];name='';ngOnInit(){this.load()}load(){this.api.categorias().subscribe(r=>this.cats=r.categorias||[])}create(){this.api.crearCategoria({nombre:this.name}).subscribe({next:()=>{this.name='';this.load()},error:e=>alert(e?.error?.mensaje||'No se pudo crear.')})}remove(id:number){if(confirm('¿Eliminar categoría?'))this.api.eliminarCategoria(id).subscribe(()=>this.load())}}