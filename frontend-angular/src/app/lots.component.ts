import {Component,inject} from '@angular/core'; import {CommonModule} from '@angular/common'; import {FormsModule} from '@angular/forms'; import {ApiService} from './api.service';
@Component({standalone:true,imports:[CommonModule,FormsModule],template:`
<div class="page-head"><div><div class="eyebrow">INVENTARIO</div><h1>Lotes y FEFO</h1><p>Entradas, devoluciones y movimientos de inventario.</p></div><button class="btn primary" (click)="open=true">+ Ingresar lote</button></div>
<section class="panel"><div class="panel-head"><div><b>Lotes</b><small>Ordenados por vencimiento</small></div></div><div class="table-scroll"><table><thead><tr><th>Medicamento</th><th>Lote</th><th>Vencimiento</th><th>Stock</th><th>Costo</th><th>Estado</th></tr></thead><tbody><tr *ngFor="let l of lots"><td><b>{{l.nombre_comercial}}</b></td><td>{{l.numero_lote}}</td><td>{{l.fecha_vencimiento|date:'dd/MM/yyyy'}}</td><td>{{l.cantidad}}</td><td>{{money(l.costo_unitario)}}</td><td><span class="badge" [class.bad]="l.dias<0||l.cantidad<=0" [class.warn]="l.dias>=0&&l.dias<=90" [class.green]="l.dias>90&&l.cantidad>0">{{l.dias<0?'Vencido':l.cantidad<=0?'Agotado':l.dias+' días'}}</span></td></tr></tbody></table></div></section>
<div class="modal-backdrop" *ngIf="open"><div class="modal"><div class="modal-head"><b>Ingreso de lote</b><button (click)="open=false">×</button></div><form (ngSubmit)="save()"><div class="form-grid"><label>Medicamento<select [(ngModel)]="form.medicamento_id" name="med"><option [ngValue]="0">Selecciona</option><option *ngFor="let m of data?.medicamentos" [ngValue]="m.id">{{m.nombre_comercial}}</option></select></label><label>Proveedor<select [(ngModel)]="form.proveedor_id" name="prov"><option [ngValue]="null">Sin proveedor</option><option *ngFor="let p of data?.proveedores" [ngValue]="p.id">{{p.nombre}}</option></select></label><label>Número de lote<input [(ngModel)]="form.numero_lote" name="lote" required></label><label>Fecha ingreso<input type="date" [(ngModel)]="form.fecha_ingreso" name="ing"></label><label>Fecha vencimiento<input type="date" [(ngModel)]="form.fecha_vencimiento" name="ven" required></label><label>Cantidad<input type="number" [(ngModel)]="form.cantidad" name="cant" min="1"></label><label>Costo unitario<input type="number" step=".01" [(ngModel)]="form.costo_unitario" name="costo"></label></div><div class="modal-actions"><button type="button" class="btn ghost" (click)="open=false">Cancelar</button><button class="btn primary">Registrar lote</button></div></form></div></div>
`}) export class LotsComponent {api=inject(ApiService);lots:any[]=[];data:any;open=false;form:any={cantidad:1,costo_unitario:0};ngOnInit(){this.load()}load(){this.api.lotes().subscribe(r=>{this.data=r;this.lots=r.lotes||[]})}save(){this.api.ingresarLote(this.form).subscribe({next:()=>{this.open=false;this.form={cantidad:1,costo_unitario:0};this.load()},error:e=>alert(e?.error?.mensaje||'No se pudo registrar el lote.')})}money(v:any){return new Intl.NumberFormat('es-BO',{style:'currency',currency:'BOB'}).format(Number(v||0))}}