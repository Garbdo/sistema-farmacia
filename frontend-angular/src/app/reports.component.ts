import {Component,inject} from '@angular/core'; import {CommonModule} from '@angular/common'; import {FormsModule} from '@angular/forms'; import {ApiService} from './api.service';
@Component({standalone:true,imports:[CommonModule,FormsModule],template:`
<div class="page-head"><div><div class="eyebrow">ANÁLISIS</div><h1>Reportes</h1><p>Indicadores construidos desde las consultas SQL del backend.</p></div><select class="period" [(ngModel)]="periodo" (change)="load()"><option value="diario">Diario</option><option value="semanal">Semanal</option><option value="mensual">Mensual</option></select></div>
<div class="stat-grid"><article class="stat-card"><span class="stat-icon green">Bs</span><div><small>VENTAS</small><strong>{{money(total)}}</strong><em>Periodo {{periodo}}</em></div></article><article class="stat-card"><span class="stat-icon blue">▣</span><div><small>INVENTARIO</small><strong>{{money(data?.valor_inventario)}}</strong><em>Valor a costo</em></div></article><article class="stat-card"><span class="stat-icon amber">#</span><div><small>OPERACIONES</small><strong>{{operations}}</strong><em>Ventas realizadas</em></div></article></div>
<section class="panel"><div class="panel-head"><div><b>Desglose de ventas</b><small>Importes por día</small></div></div><table><thead><tr><th>Fecha</th><th>Operaciones</th><th>Total</th></tr></thead><tbody><tr *ngFor="let v of data?.ventas_corte"><td>{{v.dia | date:'dd/MM/yyyy'}}</td><td>{{v.operaciones}}</td><td><b>{{money(v.total)}}</b></td></tr></tbody></table></section>
<section class="two-panels"><div class="panel"><div class="panel-head"><b>Productos más vendidos</b></div><div class="rank-row" *ngFor="let p of data?.productos_top;let i=index"><span class="rank">{{i+1}}</span><div><b>{{p.nombre_comercial}}</b><small>{{p.unidades}} unidades</small></div><strong>{{money(p.total)}}</strong></div></div><div class="panel"><div class="panel-head"><b>Próximos a caducar</b></div><div class="expiry" *ngFor="let l of data?.proximos_caducar"><div><b>{{l.nombre_comercial}}</b><small>Lote {{l.numero_lote}}</small></div><span class="badge" [class.bad]="l.dias<=30" [class.warn]="l.dias>30">{{l.dias}} días</span></div></div></section>
`}) export class ReportsComponent {api=inject(ApiService);data:any;periodo='mensual';ngOnInit(){this.load()}load(){this.api.reportes(this.periodo).subscribe(r=>this.data=r)}get total(){return (this.data?.ventas_corte||[]).reduce((a:any,v:any)=>a+Number(v.total||0),0)}get operations(){return (this.data?.ventas_corte||[]).reduce((a:any,v:any)=>a+Number(v.operaciones||0),0)}money(v:any){return new Intl.NumberFormat('es-BO',{style:'currency',currency:'BOB'}).format(Number(v||0))}}