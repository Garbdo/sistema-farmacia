import {Component,inject} from '@angular/core'; import {CommonModule} from '@angular/common'; import {ApiService} from './api.service';
@Component({standalone:true,imports:[CommonModule],template:`<div class="page-head"><div><div class="eyebrow">SEGURIDAD</div><h1>Usuarios</h1><p>Usuarios y estado de acceso del sistema.</p></div></div><section class="panel"><div class="panel-head"><div><b>Usuarios registrados</b><small>Administración de accesos</small></div></div><table><thead><tr><th>Usuario</th><th>Nombre</th><th>Rol</th><th>Estado</th><th></th></tr></thead><tbody><tr *ngFor="let u of users"><td><b>{{u.usuario}}</b></td><td>{{u.nombre}}</td><td><span class="badge blue">{{u.rol}}</span></td><td><span class="badge" [class.green]="u.activo" [class.bad]="!u.activo">{{u.activo?'Activo':'Inactivo'}}</span></td><td><button class="link-btn" (click)="toggle(u.id)">{{u.activo?'Desactivar':'Activar'}}</button></td></tr></tbody></table></section>`})
export class UsersComponent {api=inject(ApiService);users:any[]=[];ngOnInit(){this.load()}load(){this.api.usuarios().subscribe(r=>this.users=r.usuarios||[])}toggle(id:number){this.api.toggleUsuario(id).subscribe(()=>this.load())}}